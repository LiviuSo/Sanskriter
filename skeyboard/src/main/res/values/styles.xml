<resources>

    <!-- Base application theme. -->
    <style name="Theme.Keyboard" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="KeyStyle">
        <item name="android:layout_marginStart">3dp</item>
        <item name="android:layout_marginLeft">3dp</item>
        <item name="android:layout_marginTop">3dp</item>
        <item name="android:gravity">center</item>
        <item name="android:padding">5dp</item>
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">#CBDACB</item>
        <item name="android:textAllCaps">false</item>
        <item name="android:background">#171917</item>
    </style>

    <style name="KeyStyleRight" parent="KeyStyle">
        <item name="android:layout_marginEnd">2dp</item>
        <item name="android:layout_marginRight">2dp</item>
    </style>

    <style name="KeyStyleBottom" parent="KeyStyle">
        <item name="android:layout_marginBottom">2dp</item>
    </style>

    <style name="KeyStyleRightBottom" parent="KeyStyle">
        <item name="android:layout_marginEnd">2dp</item>
        <item name="android:layout_marginRight">2dp</item>
        <item name="android:layout_marginBottom">2dp</item>
    </style>
</resources>
