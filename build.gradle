// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript { 
    ext.kotlin_version = '1.3.41'

    // versions
    ext.kotlinVersion = '1.3.41'
    ext.supportVersion = '28.0.0'
    ext.gradleVersion = '3.5.0-rc03'
    ext.appcompatVersion = '1.1.0-rc01'
    ext.junitVersion = '4.13-beta-3'
    ext.testRunnerVersion = '1.2.0'
    ext.espressoCoreVersion = '3.2.0'
    ext.constraintLayoutVersion = '2.0.0-beta2'
    ext.materialVersion = '1.1.0-alpha09'
    ext.archLifecycleVersion = '2.2.0-alpha02'
    ext.roomVersion = '2.1.0'
    ext.zoomImageVersion = '1.1.0'
    ext.gsonVersionVersion = '2.8.5'
    ext.recViewVersion = '1.1.0-beta01'
    ext.rxAndroidVersion = '2.1.1'
    ext.rxJavaVersion = '2.2.7'
    ext.kotlinReflectVersion = '1.3.31'

    repositories {
        google()
        jcenter()
    }

    dependencies {
        classpath "com.android.tools.build:gradle:$gradleVersion"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlinVersion"

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version" }
}

allprojects {
    repositories {
        google()
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
